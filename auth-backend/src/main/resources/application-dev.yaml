server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/AuthDb
    username: Daenish
    password: Daenish

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

